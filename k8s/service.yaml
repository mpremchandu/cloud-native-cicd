apiVersion: v1
kind: Service
metadata:
  name: cloud-native-app-service
spec:
  type: NodePort
  selector:
    app: cloud-native-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080 # Exposes the app outside the cluster on this port
